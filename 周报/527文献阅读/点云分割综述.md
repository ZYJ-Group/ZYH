语义分割：标签分配给2D或3D中每个点。

相机传感器的2D——3D激光雷达点云（内存消耗大，环境要求高，点云密度越远越稀疏，数据集少）

**方法**：基于投影（3D激光雷达点云投影到2D平面上，处理后的数据回3D域），

​            基于voxel-based（点云映射到类似于图像处理中使用的2D笛卡尔坐标网格的网格中。体素的值

是通过取位于体素内的点的强度的平均值来计算的，较小对象不能使用大分辨率）

​           基于点（直接对原始点云进行操作，几何属性保留，MLP独立处理每个点，最大池操作提取全局特征，点集划分为局部区域来解决此问题）

**主要特征：**

稀疏卷积：活动输入点的卷积，非零单元                

注意力机制：元素权重学习

生成对抗网络：生成器G（噪声z空间输入）和鉴别器D（分类真假）组成，这是两个相互竞争的神经网络模型

**点云语义分割：**

结合投影：**PolarNet** （自上而下，传感器附近更多包含更多点，使用极坐标网格）

​             距离视图：将3D点映射到2D平面，HW5

结合3D：voxel-based ，基于半径ρ和方位角θ，将点从笛卡尔坐标（x，y，z）转换到圆柱坐标系（ρ，θ，z）

混合：RV➕BEV

数据集：**2D3DNet**：用2D语义分割网络的预测来创建伪地面真实，伪地面真实又用于监督3D分割模型的训练，解决不平衡，少        **SemanticKITTI和nuScenes数据集**

 步骤：DeepLabv3在2D语义分割中作为主干，多视图融合中对激光雷达点云进行标记（时间滤波器），3D稀疏卷积网络训练实际的3D分割模型

**RPVNet**：四个下采样和四个上采样块组成

1，体素到点V2P（八个相邻体素的三线性插值）和距离到点R2P（于四个相邻像素的双线性插值）操作，其将基于体素和基于距离的特征两者变换为点表示，搜索相应的体素或像素

2，门控融合模块(GFM)融合三个点表示，权重

3，训练集对象按类别等采样，随机缩放和旋转

不同方法，消融实验

<img src="https://raw.githubusercontent.com/ZYJ-Group/ZYH/main/image-20260602023128782.png" alt="image-20260602023128782" style="zoom:80%;" />



